import { FC } from 'react';
import { CatalogPage } from '../CatalogPage/CatalogPage';

const CartPage: FC = (): JSX.Element => {
  return <CatalogPage smallTitle='Cart' mainTitle='Products Cart' models='' />;
};

export { CartPage };
